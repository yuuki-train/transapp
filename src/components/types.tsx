export type Trains = {
    id: string
    departure: string
    depHour: number
    depMinute: number
    destination: string
    arvHour: number
    arvMinute: number
    trainType: string
    fee: number
}